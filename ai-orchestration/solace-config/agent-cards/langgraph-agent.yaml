# Agent card: LangGraph agent (self-description for SAM discovery)
# Placeholder; replace endpoint with actual LangGraph service URL when running.

name: langgraph-code-agent
description: "LangGraph agent for code execution and tool use"
capabilities:
  - code_execution
  - reasoning
  - tool_calling
topic_subscriptions:
  - "a2a/code-execution/#"
endpoint: "http://langgraph-agent:8080"
protocol: "a2a"
version: "v1"
