---
# Install: ansible-galaxy collection install -r collections/requirements.yml
collections:
  - name: cisco.ios
    version: ">=6.0.0"
  - name: ansible.netcommon
    version: ">=5.0.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.docker
    version: ">=3.0.0"
  # Optional: Helm on K3s (playbooks/opennebula-hybrid-site.yml, role opennebula_k3s_gitea)
  - name: kubernetes.core
    version: ">=2.4.0"
