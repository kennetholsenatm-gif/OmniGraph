# Non-secret defaults for DevSecOps. Secrets go in devsecops_secrets (Ansible Vault or Vault).
# See devsecops.env.schema for full list. Encrypt devsecops_secrets with: ansible-vault encrypt group_vars/ai_mesh_nodes/devsecops_secrets.yml

# Paths (override if playbook runs from different location)
# Paths are relative to playbook_dir (repo/ansible/playbooks). Override if running from elsewhere.
# devsecops_compose_base: "C:/GiTeaRepos/devsecops-pipeline/docker-compose"
# devsecops_gateway_base: "C:/GiTeaRepos/devsecops-pipeline/single-pane-of-glass"

# Optional: HashiCorp Vault (for start-containers-with-vault.yml)
# vault_addr: "http://vault:8200"
# vault_token: "{{ lookup('env', 'VAULT_TOKEN') }}"
