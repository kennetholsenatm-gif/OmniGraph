---
# Mini PC running AlmaLinux + Incus (VyOS LXC, PacketFence VM, RatTrap VLANs).
# Copy to inventory/mini-pc-firewall.yml (gitignored if secrets) and set ansible_host.

all:
  children:
    mini_pc_firewall_hosts:
      hosts:
        mini-pc-fw-01:
          ansible_host: 192.0.2.10
          ansible_user: root
          # ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          # mini_pc_incus_install_method: copr
          # mini_pc_incus_copr: neelc/incus
