# Gitleaks — secret scanning (pre-commit + CI). Tune allowlists for false positives.
title = "omnigraph"

[allowlist]
description = "Docs and examples often contain token-shaped strings"
paths = [
  '''.*\.md$''',
  '''.*\.example\.ya?ml$''',
  '''docs/snippets/''',
]
