# Optional: merge or include when Grafana and OpenSearch Dashboards backends exist.
# Copy to routes.yml or add as second file in dynamic/ (Traefik merges all YAML in the directory).
# Ensure Traefik is attached to the networks where grafana and opensearch-dashboards run.

# http:
#   routers:
#     grafana:
#       rule: "PathPrefix(`/grafana`)"
#       priority: 90
#       service: grafana
#       entryPoints: [web]
#       middlewares: [strip-grafana]
#     opensearch-dashboards:
#       rule: "PathPrefix(`/opensearch`)"
#       priority: 90
#       service: opensearch-dashboards
#       entryPoints: [web]
#       middlewares: [strip-opensearch]
#   middlewares:
#     strip-grafana:
#       stripPrefix: { prefixes: ["/grafana"] }
#     strip-opensearch:
#       stripPrefix: { prefixes: ["/opensearch"] }
#   services:
#     grafana:
#       loadBalancer:
#         servers: [{ url: "http://grafana:3000" }]
#         passHostHeader: true
#     opensearch-dashboards:
#       loadBalancer:
#         servers: [{ url: "http://opensearch-dashboards:5601" }]
#         passHostHeader: true
