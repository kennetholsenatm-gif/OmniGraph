[project]
name = "omnigraph"
version = "0.0.0"
description = "OmniGraph public repository metadata"
readme = "README.md"
requires-python = ">=3.11"

[tool.black]
line-length = 100
target-version = ["py311"]
