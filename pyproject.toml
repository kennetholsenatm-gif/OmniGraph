[project]
name = "devsecops-pipeline"
version = "0.0.0"
description = "DevSecOps pipeline repo (Ansible, OpenTofu, scripts)"
readme = "README.md"
requires-python = ">=3.11"

[tool.black]
line-length = 100
target-version = ["py311"]
