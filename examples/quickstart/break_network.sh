#!/usr/bin/env bash
# Example external "outage" trigger for practicing observation in OmniGraph (throwaway / lab only).
# OmniGraph does not execute this script; you run it yourself while the workspace is open.
#
# Replace the body with something appropriate for your stack, for example:
#   - stop a local mock API or dependency container
#   - apply a deliberate Terraform/OpenTofu change in a disposable workspace
#   - use firewall rules in a lab VM (never copy-paste production rules from the internet)
set -euo pipefail
echo "break_network.sh: edit this script for your lab, then run it while watching Topology + SSE in OmniGraph."
exit 0
