# Getting Started

Canonical source: `README.md` and `docs/development/local-dev.md`.

## Quick start

```bash
go build -o bin/omnigraph ./cmd/omnigraph
./bin/omnigraph --help
cd web
npm ci
npm run dev
```
